resolver kube-dns.kube-system.svc.cluster.local valid=30s;

server {
    listen       80;

    location ~ ^/$ {
        # TODO we want to remove the port number from this
        return 301 http://localhost:5000/dashboard/;
    }

    location /api/ {
        proxy_pass_request_headers on;
        proxy_pass http://dashboard-api-service.default.svc.cluster.local:80/;
    }

    location /dashboard/ {
        proxy_pass http://dashboard-ui-service.default.svc.cluster.local:80/;
    }
}
